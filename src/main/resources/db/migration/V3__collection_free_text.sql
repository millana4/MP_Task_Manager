-- Коллекция карточки теперь хранится как есть (текст от парсера:
-- "Весна-лето 2026", "Весна", "Лето" и т.д.), а не как три категории.
-- Парсер сам разбирает эти значения при переподборе.

ALTER TABLE card DROP CONSTRAINT IF EXISTS card_collection_check;